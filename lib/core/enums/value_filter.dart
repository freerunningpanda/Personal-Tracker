/// [ValueFilter] is an enum class that contains the value filter options.
enum ValueFilter {
  /// The value filter option that represents the "from less" filter.
  less,

  /// The value filter option that represents the "from more" filter.
  more,
}
