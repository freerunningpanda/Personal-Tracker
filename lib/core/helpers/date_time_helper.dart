import 'package:intl/intl.dart';

/// [DateTimeHelper] is a helper class.
class DateTimeHelper {
  const DateTimeHelper._();

  /// [dateFormat] is a [DateFormat] object.
  static final dateFormat = DateFormat('yyyy-MM-dd');
}
